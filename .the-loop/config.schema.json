{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/MadaraUchiha-314/the-loop/.the-loop/config.schema.json",
  "title": "the-loop configuration",
  "description": "Schema for .the-loop/config.yaml. A subset of these keys may be overridden per work-item via the YAML front-matter of the work-item / spec markdown.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "ticketing", "repository", "reviews"],
  "$defs": {
    "autonomyPolicy": {
      "type": "string",
      "enum": ["autonomous-complete", "human-approves-pr", "human-approves-spec-and-pr"],
      "description": "What a risk tier requires before completion: finish autonomously after the review loop, or require a human to approve the PR (and optionally the spec)."
    }
  },
  "properties": {
    "version": {
      "type": "string",
      "description": "Schema version of this config file. Used by /upgrade-the-loop to reconcile.",
      "examples": ["0.1.0"]
    },
    "ticketing": {
      "type": "object",
      "description": "Where work items live.",
      "additionalProperties": false,
      "required": ["system"],
      "properties": {
        "system": {
          "type": "string",
          "enum": ["github", "jira"],
          "default": "github"
        },
        "github": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "owner": { "type": "string" },
            "repo": { "type": "string" },
            "useProjects": { "type": "boolean", "default": true }
          }
        },
        "jira": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "baseUrl": { "type": "string", "format": "uri" },
            "projectKey": { "type": "string" }
          }
        }
      }
    },
    "repository": {
      "type": "object",
      "description": "Repository layout and tooling.",
      "additionalProperties": false,
      "required": ["monorepo"],
      "properties": {
        "monorepo": { "type": "boolean", "default": true },
        "monorepoTool": {
          "type": "string",
          "enum": ["nx", "yarn", "pnpm", "bun", "none"],
          "default": "nx",
          "description": "Only meaningful when monorepo is true. Default is Nx; the-loop must also work when monorepo is false."
        },
        "runScriptsFromRoot": {
          "type": "boolean",
          "default": true,
          "description": "All scripts are invoked from the project root."
        }
      }
    },
    "workflow": {
      "type": "object",
      "description": "The Kiro-style 3-phase spec workflow and the phase state machine tracked on the ticket.",
      "additionalProperties": false,
      "properties": {
        "specApproach": {
          "type": "string",
          "enum": ["kiro-3-phase"],
          "default": "kiro-3-phase",
          "description": "(optional brainstorm.md ->) requirements(.md)/bugfix(.md) -> design.md -> tasks.md, each artifact iterated with feedback and gated by a human review before advancing."
        },
        "requireHumanReviewPerPhase": {
          "type": "boolean",
          "default": true,
          "description": "A human review/approval is required at the end of each spec phase."
        },
        "specDir": {
          "type": "string",
          "default": "docs/specs",
          "description": "Per-work-item spec files live in <specDir>/<id>/{requirements|bugfix,design,tasks,execution-log}.md."
        },
        "capabilitiesDir": {
          "type": "string",
          "default": "docs/capabilities",
          "description": "Living capability docs (the organized view of specs; single source of truth for a capability's current behaviour) live in <capabilitiesDir>/, indexed by <capabilitiesDir>/capabilities.md. Affected docs are updated in the same PR as the work item (ready-to-ship gate item)."
        },
        "phaseLabelPrefix": {
          "type": "string",
          "default": "loop:",
          "description": "Prefix for the labels/tags created in the ticketing system to track phase."
        },
        "phases": {
          "type": "array",
          "description": "Ordered phase state machine for a work item. Each maps to a ticket label. `brainstorming` is optional — a work item may enter it or start directly at `requirements-definition`.",
          "default": [
            "not-started",
            "brainstorming",
            "requirements-definition",
            "design",
            "tasks-breakdown",
            "implementation",
            "needs-review",
            "complete"
          ],
          "items": {
            "type": "string",
            "enum": [
              "not-started",
              "brainstorming",
              "requirements-definition",
              "design",
              "tasks-breakdown",
              "implementation",
              "needs-review",
              "complete"
            ]
          }
        }
      }
    },
    "tooling": {
      "type": "object",
      "description": "Per-language tooling. RULE: CI/CD must use exactly the same tooling as local.",
      "additionalProperties": false,
      "properties": {
        "languages": {
          "type": "array",
          "items": { "type": "string", "enum": ["python", "js", "ts", "go"] },
          "uniqueItems": true
        },
        "packageManager": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "python": { "type": "string", "default": "uv" },
            "js": { "type": "string", "default": "bun" },
            "ts": { "type": "string", "default": "bun" },
            "go": { "type": "string", "default": "go" }
          }
        },
        "unitTest": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "python": { "type": "string", "default": "pytest" },
            "js": { "type": "string", "default": "vitest" },
            "ts": { "type": "string", "default": "vitest" },
            "go": { "type": "string", "default": "go test" }
          }
        },
        "integrationTest": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "python": { "type": "string", "default": "pytest" },
            "js": { "type": "string", "default": "playwright" },
            "ts": { "type": "string", "default": "playwright" },
            "go": { "type": "string", "default": "go test" }
          }
        },
        "lint": {
          "type": "object",
          "description": "RULE: all files including markdown should be linted.",
          "additionalProperties": false,
          "properties": {
            "python": { "type": "string", "default": "ruff" },
            "js": { "type": "string", "default": "oxlint" },
            "ts": { "type": "string", "default": "oxlint" },
            "go": { "type": "string", "default": "golangci-lint" },
            "markdown": { "type": "string", "default": "markdownlint" }
          }
        },
        "typeCheck": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "python": { "type": "string", "default": "pyright" },
            "ts": { "type": "string", "default": "tsc" },
            "go": { "type": "string", "default": "go build" }
          }
        },
        "release": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "js": { "type": "string", "default": "npm" },
            "ts": { "type": "string", "default": "npm" },
            "python": { "type": "string", "default": "pypi" },
            "go": { "type": "string", "default": "go-modules" },
            "containers": { "type": "string", "default": "ghcr" }
          }
        }
      }
    },
    "testing": {
      "type": "object",
      "description": "Integration-test scenario conventions and how the harness queries them. See reference/testing.md.",
      "additionalProperties": false,
      "properties": {
        "gherkinDocstrings": {
          "type": "string",
          "enum": ["required", "recommended", "off"],
          "default": "required",
          "description": "RULE: every integration test carries a Gherkin-syntax docstring (Feature/Scenario/Given-When-Then) naming the scenario under test."
        },
        "linkRequirements": {
          "type": "boolean",
          "default": true,
          "description": "When a test is tied to a requirements.md, its docstring includes a `Requirement:` link to that file (and section anchor)."
        },
        "integrationTestGlobs": {
          "type": "array",
          "description": "Globs the harness scans to enumerate integration-test scenarios (`the-loop scenarios`). Empty = use the CLI's built-in defaults.",
          "items": { "type": "string" }
        }
      }
    },
    "apiSpecs": {
      "type": "object",
      "description": "Where API contracts are authored and how docs are generated from them. See reference/testing.md (API specs).",
      "additionalProperties": false,
      "properties": {
        "rest": {
          "type": "object",
          "additionalProperties": false,
          "description": "RESTful API contracts.",
          "properties": {
            "dir": {
              "type": "string",
              "default": "specs/openapi",
              "description": "RULE: all RESTful API specs are authored here in the OpenAPI format."
            },
            "format": {
              "type": "string",
              "enum": ["openapi-3.1", "openapi-3.0"],
              "default": "openapi-3.1"
            },
            "generateDocs": {
              "type": "boolean",
              "default": true,
              "description": "Documentation is generated automatically from the OpenAPI specs."
            },
            "docsTool": {
              "type": "string",
              "default": "redocly",
              "description": "Tool that renders docs from OpenAPI. Free-form; e.g. redocly, widdershins, swagger-ui."
            }
          }
        },
        "graphql": {
          "type": "object",
          "additionalProperties": false,
          "description": "GraphQL best practice: an SDL-first schema, checked in, linted/snapshot-tested, and documented.",
          "properties": {
            "dir": {
              "type": "string",
              "default": "specs/graphql",
              "description": "Where the checked-in GraphQL schema lives."
            },
            "schemaFormat": {
              "type": "string",
              "enum": ["sdl"],
              "default": "sdl",
              "description": "Schema Definition Language — the human-authored, checked-in, snapshot-tested contract."
            },
            "generateDocs": {
              "type": "boolean",
              "default": true,
              "description": "Documentation is generated automatically from the SDL schema."
            },
            "docsTool": {
              "type": "string",
              "default": "spectaql",
              "description": "Tool that renders docs from the SDL. Free-form; e.g. spectaql, graphdoc, magidoc."
            }
          }
        }
      }
    },
    "design": {
      "type": "object",
      "description": "How UI/UX (visual) design is captured and iterated in the design phase, alongside design.md's architecture/HLD/LLD. See reference/design-artifacts.md.",
      "additionalProperties": false,
      "properties": {
        "uiArtifacts": {
          "type": "object",
          "additionalProperties": false,
          "description": "Visual design artifacts for work items with a user-facing surface. First-class, tracked design-phase artifacts (siblings of design.md), iterated-until-locked with the designer persona and referenced from the ticket. Work items with no UI simply produce none.",
          "properties": {
            "dir": {
              "type": "string",
              "default": "design",
              "description": "Directory (relative to the work item's spec dir, <specDir>/<id>/) holding checked-in UI/UX design artifacts, e.g. docs/specs/<id>/design/."
            },
            "format": {
              "type": "string",
              "enum": ["html", "figma", "both"],
              "default": "html",
              "description": "Preferred representation for generated visual design: self-contained HTML+CSS+JS prototypes (Claude-artifact style), a linked Figma file, or both."
            },
            "selfContained": {
              "type": "boolean",
              "default": true,
              "description": "RULE for HTML prototypes: self-contained (inline CSS/JS, data-URI assets, no external network deps) so they render standalone in a browser and as a Claude-style artifact."
            },
            "screenshotEvidence": {
              "type": "boolean",
              "default": true,
              "description": "Capture rendered screenshots of the locked design artifacts as design-phase evidence, embedded in the reviewer briefing."
            }
          }
        }
      }
    },
    "localOrchestration": {
      "type": "object",
      "description": "Multi-artifact & multi-entity testing.",
      "additionalProperties": false,
      "properties": {
        "containerRuntime": { "type": "string", "default": "podman" },
        "linkPackagesLocally": { "type": "boolean", "default": true },
        "remoteServices": {
          "type": "array",
          "description": "Services that should point at a remote instead of running locally.",
          "items": { "type": "string" }
        }
      }
    },
    "hooks": {
      "type": "object",
      "description": "Pre-commit / pre-push gating. RULE: same tooling as CI.",
      "additionalProperties": false,
      "properties": {
        "preCommit": {
          "type": "array",
          "items": { "type": "string", "enum": ["lint", "typecheck", "unit-test"] },
          "default": ["lint", "typecheck", "unit-test"]
        },
        "prePush": {
          "type": "array",
          "items": { "type": "string", "enum": ["lint", "typecheck", "unit-test", "integration-test"] },
          "default": ["lint", "typecheck", "unit-test"]
        },
        "commitConvention": {
          "type": "string",
          "enum": ["conventional-commits", "none"],
          "default": "conventional-commits",
          "description": "Commit-message convention enforced via a commit-msg hook. RULE: all commits are Conventional Commits (https://www.conventionalcommits.org/en/v1.0.0/)."
        }
      }
    },
    "observability": {
      "type": "object",
      "description": "Logging must be identical at dev-time and run-time; only breakpoints differ.",
      "additionalProperties": false,
      "properties": {
        "devLevel": { "type": "string", "enum": ["debug", "info", "warn", "error"], "default": "debug" },
        "runtimeLevel": { "type": "string", "enum": ["debug", "info", "warn", "error"], "default": "info" },
        "browserLogging": {
          "type": "string",
          "description": "How browser logs are surfaced to the harness, e.g. chrome-devtools-mcp.",
          "default": "chrome-devtools-mcp"
        }
      }
    },
    "reviews": {
      "type": "object",
      "description": "Self-review and critic-review loop before escalating to a human. The counts are caps; see reference/reviewing.md for the procedure they drive.",
      "additionalProperties": false,
      "required": ["selfReviewCount", "criticReviewCount"],
      "properties": {
        "selfReviewCount": {
          "type": "integer",
          "minimum": 0,
          "default": 3,
          "description": "Max self-review rounds (a cap; the loop stops early when a round yields zero new actionable findings)."
        },
        "criticReviewCount": {
          "type": "integer",
          "minimum": 0,
          "default": 3,
          "description": "Max critic-review rounds (a cap; also stops early on zero new findings)."
        },
        "stopOnNoNewFindings": {
          "type": "boolean",
          "default": true,
          "description": "Stop the review loop as soon as a round surfaces no new actionable finding."
        },
        "escalateOnRepeatFinding": {
          "type": "boolean",
          "default": true,
          "description": "Diminishing-returns guard: if two consecutive rounds surface the SAME finding, escalate to a human instead of looping."
        },
        "critics": {
          "type": "array",
          "description": "Alternate harness/model combos used as critics, e.g. cursor+gpt-5.5 reviewing claude-opus output.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["name"],
            "properties": {
              "name": { "type": "string" },
              "harness": {
                "type": "string",
                "description": "Critic harness/CLI. Free-form so any harness works; known examples: claude, cursor, aider, continue, a local model runner."
              },
              "model": { "type": "string" },
              "command": {
                "type": "string",
                "description": "Optional explicit invocation for an arbitrary critic harness (how to run it)."
              }
            }
          }
        }
      }
    },
    "autonomy": {
      "type": "object",
      "description": "Risk-tiered autonomy: how much a work item may complete without a human, by risk tier (1-Trivial … 5-Critical). See reference/workflow.md.",
      "additionalProperties": false,
      "properties": {
        "defaultTier": {
          "type": "integer",
          "minimum": 1,
          "maximum": 5,
          "default": 3,
          "description": "Risk tier assumed when a work item does not declare `riskTier` in its front-matter."
        },
        "inferFromChange": {
          "type": "boolean",
          "default": true,
          "description": "Raise the effective tier automatically when the change touches sensitive areas (auth/security/schema/public API)."
        },
        "sensitivePaths": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Globs that force a high tier when touched, e.g. **/auth/**, **/*secret*, **/*schema*."
        },
        "tiers": {
          "type": "object",
          "description": "Per-tier gate policy (keys \"1\"–\"5\"). Lower tiers may complete autonomously after the review loop; higher tiers require the named human gate.",
          "additionalProperties": false,
          "properties": {
            "1": { "$ref": "#/$defs/autonomyPolicy" },
            "2": { "$ref": "#/$defs/autonomyPolicy" },
            "3": { "$ref": "#/$defs/autonomyPolicy" },
            "4": { "$ref": "#/$defs/autonomyPolicy" },
            "5": { "$ref": "#/$defs/autonomyPolicy" }
          }
        }
      }
    },
    "tdd": {
      "type": "object",
      "description": "Test-driven-development discipline in the implementation phase. See reference/workflow.md.",
      "additionalProperties": false,
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["standard", "tdd-first", "off"],
          "default": "standard",
          "description": "standard = red→green→refactor per task; tdd-first = all tests for the work item written and failing before any production code; off = no enforced test-first."
        }
      }
    },
    "minimalism": {
      "type": "object",
      "description": "Generation-time minimalism pass to counter code bloat. Advisory: informs generation, never gates a merge and never trades away validation/error-handling/security/accessibility. See reference/minimalism.md.",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean", "default": true },
        "intensity": {
          "type": "string",
          "enum": ["low", "standard", "high"],
          "default": "standard"
        }
      }
    },
    "selfImprovement": {
      "type": "object",
      "description": "Lifecycle for learnings: capture → write-gate → consolidate → inject. See reference/automation.md.",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean", "default": true },
        "maxIndexLines": {
          "type": "integer",
          "minimum": 1,
          "default": 200,
          "description": "Cap on the injected learnings index; overflow detail spills to learnings/topics/<category>.md, read on demand."
        },
        "writeGateOccurrences": {
          "type": "integer",
          "minimum": 1,
          "default": 3,
          "description": "A learning candidate becomes durable only after recurring this many times (rule-of-three); a clearly high-severity one-off may bypass the gate."
        }
      }
    },
    "userInteraction": {
      "type": "object",
      "description": "How the-loop communicates with humans when requesting input and presenting work. RULES: always give enough context to make the call; all diagrams are mermaid; educating the user on low-level decisions is mandatory, not optional.",
      "additionalProperties": false,
      "properties": {
        "diagramFormat": {
          "type": "string",
          "enum": ["mermaid"],
          "default": "mermaid",
          "description": "RULE: all diagrams (PR summaries, design docs, education) are authored in mermaid."
        },
        "prSummary": {
          "type": "object",
          "additionalProperties": false,
          "description": "What every PR the-loop raises must contain so a human can review AI-authored work efficiently.",
          "properties": {
            "required": {
              "type": "boolean",
              "default": true,
              "description": "The reviewer briefing MUST be posted/updated in the PR (description or top comment) BEFORE requesting human review — a ready-to-ship gate item, so R10 education is triggered rather than silently skipped."
            },
            "templatePath": {
              "type": "string",
              "default": ".the-loop/templates/pr-briefing.md",
              "description": "Template the reviewer briefing is produced from."
            },
            "condensed": {
              "type": "boolean",
              "default": true,
              "description": "Present a condensed, prioritized summary telling the reviewer where to focus and in what order."
            },
            "includeDiagrams": {
              "type": "boolean",
              "default": true,
              "description": "Include mermaid diagrams explaining the low-level details of the change."
            },
            "documentInsightsAndDecisions": {
              "type": "boolean",
              "default": true,
              "description": "Document the insights from spec->implementation and every low-level decision the harness had to make, in the PR description."
            }
          }
        },
        "educateUser": {
          "type": "boolean",
          "default": true,
          "description": "RULE: intentionally and mandatorily educate the user on low-level design decisions as their familiarity with the codebase decreases. Not optional."
        }
      }
    },
    "personas": {
      "type": "array",
      "description": "People and groups available to collaborate. A user can hold multiple roles.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["handle", "roles"],
        "properties": {
          "handle": { "type": "string", "description": "GitHub handle or group, e.g. @octocat or @org/team." },
          "kind": { "type": "string", "enum": ["individual", "group"], "default": "individual" },
          "roles": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "enum": ["product-manager", "architect", "designer", "engineer", "qa", "reviewer", "approver"]
            }
          }
        }
      }
    },
    "messaging": {
      "type": "object",
      "description": "Optional channels for notifying/escalating to humans when a decision is pending.",
      "additionalProperties": false,
      "properties": {
        "channels": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["type"],
            "properties": {
              "type": { "type": "string", "enum": ["slack", "whatsapp", "email"] },
              "target": { "type": "string" }
            }
          }
        }
      }
    },
    "externalTools": {
      "type": "object",
      "description": "Tools/skills/MCPs/plugins the-loop should be aware of. Free-form, fully user-configurable.",
      "additionalProperties": false,
      "properties": {
        "referenceFile": {
          "type": "string",
          "default": ".the-loop/external-tools.md",
          "description": "Path to a free-form markdown file describing available tools/MCPs/plugins."
        },
        "notes": { "type": "string", "description": "Inline free-form description (alternative to referenceFile)." }
      }
    },
    "webhooks": {
      "type": "object",
      "description": "Config for the the-loop CLI webhook receiver(s) (e.g. `the-loop gh-webhook start`).",
      "additionalProperties": false,
      "properties": {
        "ghWebhook": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "host": { "type": "string", "default": "127.0.0.1" },
            "port": { "type": "integer", "minimum": 1, "maximum": 65535, "default": 8787 },
            "path": { "type": "string", "default": "/gh-webhook" },
            "secretEnv": {
              "type": "string",
              "default": "THE_LOOP_GH_WEBHOOK_SECRET",
              "description": "Env var holding the GitHub webhook secret for HMAC verification (never stored in config)."
            },
            "pidfile": { "type": "string", "default": ".the-loop/gh-webhook.pid" },
            "events": {
              "type": "array",
              "description": "GitHub event names of interest, e.g. pull_request_review_comment, workflow_run. Empty = accept all event types.",
              "items": { "type": "string" }
            },
            "routing": {
              "type": "object",
              "description": "Route received events to registered harness sessions via the session registry (docs/specs/issue-15/design.md, decision-016).",
              "additionalProperties": false,
              "properties": {
                "enabled": {
                  "type": "boolean",
                  "default": false,
                  "description": "Default for `gh-webhook start --route/--no-route`."
                },
                "registryDir": {
                  "type": "string",
                  "default": ".the-loop/sessions",
                  "description": "Directory of per-session registry JSON files (git-ignored)."
                },
                "defaultHarness": {
                  "type": "string",
                  "enum": ["claude", "cursor"],
                  "default": "claude",
                  "description": "Harness used when spawning a session for an unmatched event."
                },
                "spawnOnUnmatched": {
                  "type": "string",
                  "enum": ["never", "always", "labeled"],
                  "default": "never",
                  "description": "Policy for events matching no registered session: 'never' log-and-drop; 'always' spawn+register a new session; 'labeled' spawn only when the issue/PR carries autoExecuteLabel."
                },
                "autoExecuteLabel": {
                  "type": "string",
                  "default": "the-loop: auto-execute",
                  "description": "Issue/PR label that opts a work item into autonomous execution (spawnOnUnmatched: labeled). Read from the webhook payload, no API call."
                },
                "spawnWorkdir": {
                  "type": "string",
                  "default": ".",
                  "description": "Working directory for sessions spawned on unmatched events."
                },
                "runner": {
                  "type": "string",
                  "enum": ["process", "tmux"],
                  "default": "process",
                  "description": "How spawned sessions are hosted (receiver-global): 'process' = headless one-shot subprocess; 'tmux' = interactive harness TUI in a named tmux session (loop-<slug>) humans can attach to (issue-32, decision-021)."
                },
                "webTerminal": {
                  "type": "object",
                  "description": "Optional ttyd-served browser terminal onto the tmux sessions (runner: tmux). No auth of its own — access control is environmental: localhost, VPN, or the hosting provider's network (decision-021).",
                  "additionalProperties": false,
                  "properties": {
                    "enabled": {
                      "type": "boolean",
                      "default": false,
                      "description": "Serve tmux sessions over HTTP via ttyd (verified at receiver start)."
                    },
                    "host": {
                      "type": "string",
                      "default": "127.0.0.1",
                      "description": "Interface/IP ttyd binds. Keep 127.0.0.1 unless the network layer protects wider exposure."
                    },
                    "port": {
                      "type": "integer",
                      "minimum": 1,
                      "maximum": 65535,
                      "default": 7681,
                      "description": "ttyd listen port."
                    }
                  }
                },
                "maxConcurrentDispatches": {
                  "type": "integer",
                  "minimum": 1,
                  "default": 4,
                  "description": "Cap on harness dispatches running in parallel (per-session dispatch is always serialized)."
                },
                "dedupCacheSize": {
                  "type": "integer",
                  "minimum": 1,
                  "default": 1024,
                  "description": "Bounded LRU of X-GitHub-Delivery ids for at-most-once processing."
                },
                "dispatchTimeoutSeconds": {
                  "type": "integer",
                  "minimum": 1,
                  "default": 1800,
                  "description": "Timeout for a single harness resume/spawn subprocess."
                },
                "promptTemplate": {
                  "type": "string",
                  "default": ".the-loop/templates/webhook-event-prompt.md",
                  "description": "string.Template rendered into the prompt that resumes a session."
                },
                "spawnPromptTemplate": {
                  "type": "string",
                  "default": ".the-loop/templates/webhook-autoexecute-prompt.md",
                  "description": "string.Template rendered into the prompt for a newly spawned (auto-execute) session — kicks off the-loop's work-on flow."
                },
                "harnessArgs": {
                  "type": "object",
                  "description": "Extra CLI args per harness (e.g. permission flags). The dispatcher never widens permissions itself.",
                  "additionalProperties": false,
                  "properties": {
                    "claude": { "type": "array", "items": { "type": "string" } },
                    "cursor": { "type": "array", "items": { "type": "string" } }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
