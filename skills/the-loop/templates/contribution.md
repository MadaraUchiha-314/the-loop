---
type: contribution
workItem: "{{TICKET_ID}}"
status: draft                 # draft | in-review | approved (locked at scoped-plan)
approvedBy: []
collaborators: [engineer]
riskTier: 3
overrides: {}
---

# Contribution: {{TITLE}}

> The contribution loop's ONE artifact (issue-185): a scoped intervention in an
> existing work item, in place of the four-file spec chain. Ticket: {{TICKET_LINK}}.

## Goal

<!-- The goal exactly as the authorized user froze it at goal-definition — copy it,
     never paraphrase it. One sentence or a short paragraph. -->

{{GOAL}}

## Success criteria

<!-- The frozen criteria, verbatim, as checkboxes. Tick a box during implementation
     ONLY when that criterion is actually met; the verification gate blocks until
     every box is ticked. -->

- [ ] {{CRITERION}}

## Context

<!-- What was found by reading the existing item: its history, linked PRs, the
     process that produced it, the state of the code this intervention touches.
     Enough for a reviewer who has not read the whole thread. -->

## Approach

<!-- The requirements-and-design thinking, scoped: what will change, where, and why
     this way. Name trade-offs and anything deliberately NOT done. Diagram (mermaid)
     when three or more parts interact. -->

## Verification plan

<!-- How each success criterion will be proved: the commands, the environment, the
     evidence to capture. A criterion with no planned proof is a criterion that will
     block at verification. -->

## Verification results

Not yet executed — completed at the `verification` node: per-criterion command,
outcome and evidence.
