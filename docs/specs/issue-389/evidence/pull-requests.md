---
type: evidence
workItem: "github:MadaraUchiha-314/the-loop#389"
---

# Pull requests: the mention is the address, and three acts each end in the session

> Authored as pull requests are opened, gated at `reviewer-briefing`. One PR carries the
> spec chain and the implementation, iterated in place: the owner reviewed and approved
> the requirements on it, then asked for design and implementation on the same branch.

## Pull requests

| PR | Repository | Scope / tasks | Status |
|----|------------|---------------|--------|
| [#390](https://github.com/MadaraUchiha-314/the-loop/pull/390) | this one (`MadaraUchiha-314/the-loop`) | the spec chain (`brainstorm` → `requirements` → `design` → `testing-plan` → `tasks`, `decision-133`), then tasks 1–13: catalog rows, manifest and probe, `channels/verbs.py`, the roster's Slack id, the room's listen mode, the two record shapes, the two frames, the inbound pipeline, the listener and the shortcuts, `channels records`, the status lines, the operating model and the docs, the verification and reviews | open |
