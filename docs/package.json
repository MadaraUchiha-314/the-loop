{
  "name": "the-loop-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:sync": "bun run scripts/sync-content.mts",
    "docs:dev": "bun run docs:sync && vitepress dev .",
    "docs:build": "bun run docs:sync && vitepress build .",
    "docs:preview": "vitepress preview ."
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
