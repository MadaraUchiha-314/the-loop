{
  "name": "the-loop-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:sync": "node scripts/sync-content.mjs",
    "docs:dev": "npm run docs:sync && vitepress dev .",
    "docs:build": "npm run docs:sync && vitepress build .",
    "docs:preview": "vitepress preview ."
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
